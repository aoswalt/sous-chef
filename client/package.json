{
  "name": "sous-chef-client",
  "version": "1.0.0",
  "description": "Front end for Sous Chef",
  "main": "index.js",
  "repository": "git@github.com:aoswalt/sous-chef.git",
  "author": "Adam Oswalt <email@adamoswalt.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist/*",

    "prebuild": "npm run clean -s",
    "build": "npm run copy -s && npm run sass:build -s",

    "copy": "find src/ -name '*.html' -exec bash -c 's={}; d=${s/src/dist}; mkdir -p $(dirname $d); cp $s $d;' \\;",

    "sass:build": "node-sass src/scss/*.scss -o dist/",

    "watch": "npm run build -s && browser-sync start --server 'dist' --files 'dist/*'"
  },
  "devDependencies": {
    "browser-sync": "^2.18.2",
    "node-sass": "^3.13.0"
  }
}
